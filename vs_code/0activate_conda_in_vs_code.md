```powershell
C:\Users\balan\anaconda3\Scripts\activate.bat

conda activate blinker_pyblinker_validation
```

The Immediate Fix (Run this in your current cmd terminal):
1. First, run Anaconda's activation script to load Conda:
```powershell
C:\Users\balan\anaconda3\Scripts\activate.bat
```
(Your prompt should immediately change to start with `(base)`)

2. Now you can activate your specific environment:

```powershell
conda activate blinker_pyblinker_validation
```
(Your prompt will change to `(blinker_pyblinker_validation)`)
