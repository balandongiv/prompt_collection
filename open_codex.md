

• I can’t switch the current session into codex --dangerously-bypass-approvals-and-sandbox from inside the session itself. If you want that mode, you need to restart Codex with that exact CLI flag; for the
run already in progress, it isn’t needed because the required command approvals were granted and the pipeline is already running under the live runbook.


codex resume 019d1e1b-55e1-7c52-9ba5-a26e46c3175a

tmux
sudo apt update
sudo apt install tmux
